# Agent Instructions

## Repository State

- This repository currently contains only `.git/` and this `AGENTS.md`; do not assume a language, framework, package manager, test runner, or build command exists.
- Git is initialized on branch `master`, but the repo has no commits yet unless `git status` shows otherwise.
- No `README*`, root manifests, lockfiles, CI workflows, formatter/lint/typecheck configs, or repo-local OpenCode config were present when this file was created.

## Working Rules

- Before adding project-specific guidance, verify it from executable sources such as manifests, scripts, config files, CI workflows, or committed docs.
- If future docs conflict with scripts/config, trust the executable source and update this file only with verified behavior.
- Keep this file compact; remove these empty-repo notes once real project structure and commands exist.
